const showMore = document.querySelector(".show-more"), productsLength = document.querySelectorAll(".vacancy-item").length; let items = 4; showMore.addEventListener("click", e => { items += 2; const t = Array.from(document.querySelector(".vacancy-list").children), o = t.slice(0, items); o.forEach(e => e.classList.add("is-visible")), o.length === productsLength && (showMore.style.display = "none") });